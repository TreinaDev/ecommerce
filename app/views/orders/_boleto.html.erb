<div class="boleto">
    <%= form_tag('https://localhost:4000/api/pagamentos') do %>

      <%= "Valor total: #{number_to_currency(payment.installments_value)}" %>

    <%= button_tag 'Gerar boleto' %>
  <% end %>
</div>